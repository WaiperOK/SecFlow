# Release Policy

- `main` branch is releasable.
- Versioning follows `MAJOR.MINOR.PATCH`.
- Every release must include:
  - passing CI
  - updated `CHANGELOG.md`
  - security-impact note if behavior changed for findings.
